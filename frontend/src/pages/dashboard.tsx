
const Dashboard = () => {
    return (
        <h1 className=" text-white">
            Dash
        </h1>
    )
}

export default Dashboard

/* <div className="mt-4">
        {users.map((user) => (
            <div key={user.id} className="bg-white shadow overflow-hidden sm:rounded-md">
                <div className="px-4 py-5 sm:px-6">
                    <h3 className="text-lg leading-6 font-medium text-gray-900">{user.name}</h3>
                    <p className="mt-1 max-w-2xl text-sm text-gray-500">{user.username}</p>
                </div>
            </div>
        ))}
    </div> */